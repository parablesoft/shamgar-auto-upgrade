import shamgarAppVersion from 'dummy/utils/shamgar-app-version';
import { module, test } from 'qunit';

module('Unit | Utility | shamgar app version');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shamgarAppVersion();
  assert.ok(result);
});
